name: tasks_example
description: Isolated reference application for the Nodus local-first architecture.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.10.0

dependencies:
  flutter:
    sdk: flutter
  flutter_hooks: ^0.21.3+1
  flutter_mobx: ^2.3.0
  go_router: ^17.3.0
  nodus:
    path: ../..
  mobx: ^2.6.0
  supabase_flutter: ^2.16.0
  drift: ^2.34.2
dev_dependencies:
  drift_dev: ^2.34.4
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
